# Production Environment Configuration (Template)
# Copy this to .env.production and fill in real AWS values
# DO NOT commit .env.production with real credentials!

# ========================================
# DynamoDB Configuration (AWS Production)
# ========================================

# DynamoDB endpoint - LEAVE EMPTY for production AWS
# Empty/unset = use real AWS DynamoDB
# DYNAMODB_ENDPOINT=

# AWS Region - where your DynamoDB table is deployed
AWS_REGION=us-east-1

# Table name - must match your AWS DynamoDB table
DYNAMODB_TABLE_NAME=Yggdrasil

# AWS Credentials
# Option 1: Use environment variables (recommended for Amplify)
# AWS_ACCESS_KEY_ID=<from-aws-iam>
# AWS_SECRET_ACCESS_KEY=<from-aws-iam>

# Option 2: Use IAM roles (best practice for EC2/ECS/Amplify)
# Don't set AWS_ACCESS_KEY_ID or AWS_SECRET_ACCESS_KEY
# The SDK will use IAM role credentials automatically

# ========================================
# NextAuth Configuration
# ========================================

# CRITICAL: Generate a secure secret for production!
# Run: openssl rand -base64 32
# NEVER use the development secret in production!
NEXTAUTH_SECRET=CHANGE_THIS_TO_SECURE_RANDOM_STRING

# NextAuth URL - your production domain
# Update this when you deploy to AWS Amplify
NEXTAUTH_URL=https://your-app.amplifyapp.com

# ========================================
# Cognito Configuration (Production Only)
# ========================================

# AWS Cognito User Pool ID
# Get this from AWS Console > Cognito > User Pools
NEXT_PUBLIC_COGNITO_REGION=us-east-1
NEXT_PUBLIC_COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
NEXT_PUBLIC_COGNITO_CLIENT_ID=your_cognito_client_id

# ========================================
# S3 Configuration (Production)
# ========================================

# S3 bucket for storing profile photos
S3_BUCKET_NAME=yggdrasil-photos
S3_REGION=us-east-1

# ========================================
# Application Settings
# ========================================

# Node environment
NODE_ENV=production

# Disable debug logging in production
DEBUG=false

# ========================================
# Monitoring & Analytics (Optional)
# ========================================

# CloudWatch logging
# CLOUDWATCH_LOG_GROUP=/aws/yggdrasil
# CLOUDWATCH_LOG_STREAM=production

# ========================================
# Security Notes
# ========================================

# 1. NEVER commit this file with real credentials
# 2. Use AWS Systems Manager Parameter Store or Secrets Manager
# 3. Rotate credentials regularly
# 4. Use IAM roles when possible (Amplify, EC2, ECS)
# 5. Enable AWS CloudTrail for audit logging
# 6. Set up AWS Budgets to monitor costs

# ========================================
# Deployment Checklist
# ========================================

# [ ] Create DynamoDB table in AWS
# [ ] Create Cognito User Pool
# [ ] Create S3 bucket with proper CORS
# [ ] Set up IAM roles with least privilege
# [ ] Configure environment variables in Amplify
# [ ] Generate secure NEXTAUTH_SECRET
# [ ] Update NEXTAUTH_URL to production domain
# [ ] Test authentication flow
# [ ] Enable CloudWatch logging
# [ ] Set up AWS Budgets alerts
